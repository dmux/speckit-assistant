// Registers jest-dom matchers (toBeInTheDocument, toHaveClass, etc.) with Vitest's expect.
// Safe across both node and jsdom environments — it only extends the expect API.
import '@testing-library/jest-dom/vitest';
